<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
    res.redirect('/')
} %>

<%# messages() %>

<%- vehicleInfo %>

<div id="reviews">
    <h3>Costumer Reviews</h3>
    <% if (!locals.loggedin) { %>
        <p>You must <a href="/account/login">login</a> to write a review</p>
    <% } %>
</div>